services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: thecrew
      POSTGRES_PASSWORD: thecrew
      POSTGRES_DB: thecrew
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
